@*
    Editorial world section.

    Asymmetric split: oversized statement type on the left, the continuity figure on the right.

    The figure used to be a coordinate grid of map cells. It was decorative and said nothing -
    a visitor could read every square of it and learn none of what this section claims. It now
    draws the claim itself: one lane for the world, one for the instance underneath it, and the
    events that break the second without touching the first.
*@

@inject IMarketingContentService Content
@inject IStringLocalizer<HomeText> Text

<section class="world" id="world" aria-labelledby="world-heading" data-motion="scene" data-scene>
    <div class="hts-container hts-container--wide world-inner">
        <div class="world-copy" data-reveal="left">
            <p class="world-kicker" data-text-effect="flicker" data-motion-step="label">
                <span class="kicker-index">01</span>
                @Text["World.Kicker"]
            </p>

            <h2 id="world-heading" class="world-title" data-text-effect="rise" data-motion-step="headline">
                @Text["World.Line1"]<br />
                @Text["World.Line2"] <span class="world-accent">@Text["World.Accent"]</span><br />
                @Text["World.Line3"]
            </h2>

            <p class="world-body" data-motion-step="copy">@Text["World.Body1"]</p>

            <p class="world-body" data-motion-step="copy">@Text["World.Body2"]</p>

            <dl class="world-readout" data-motion-step="details">
                <div>
                    <dt>@Text["World.DataLabel"]</dt>
                    <dd>@Text["World.DataValue"]</dd>
                </div>
                <div>
                    <dt>@Text["World.RestoreLabel"]</dt>
                    <dd>@Text["World.RestoreValue"]</dd>
                </div>
                <div>
                    <dt>@Text["World.CellLabel"]</dt>
                    <dd>@Text["World.CellValue"]</dd>
                </div>
            </dl>
        </div>

        <div class="world-figure" data-reveal="right" data-motion-step="visual" data-parallax="14">
            <WorldTimeline Events="Content.WorldEvents" />
        </div>
    </div>
</section>
