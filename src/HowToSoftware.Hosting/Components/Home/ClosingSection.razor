@*
    Closing section.

    Full-bleed statement type over the same routed field used in the hero, so the page ends
    where it started and the infrastructure motif closes the loop.
*@

@inject IOptions<SiteOptions> Site

<section class="closing" id="get-started" aria-labelledby="closing-heading">
    <div class="closing-field" aria-hidden="true">
        <span class="closing-spine"></span>
        <span class="closing-glow"></span>
    </div>

    <div class="hts-container hts-container--wide closing-inner">
        <div class="closing-copy" data-reveal="left">
            <p class="closing-kicker">
                <span class="kicker-dot"></span>
                READY WHEN YOU ARE
            </p>

            <h2 id="closing-heading" class="closing-title">
                Bring your community<br />
                <span class="closing-accent">to Knox County.</span>
            </h2>

            <p class="closing-lede">
                We are onboarding the first Project Zomboid worlds now. Tell us how your group
                plays and we will get one stood up.
            </p>

            <div class="closing-actions">
                <CtaButton Href="#plans" Size="CtaSize.Large" TrailingIcon="IconName.ArrowRight">
                    Choose a plan
                </CtaButton>
                <CtaButton Href="#control-panel" Variant="CtaVariant.Secondary" Size="CtaSize.Large">
                    Explore the panel
                </CtaButton>
            </div>
        </div>

        <aside class="closing-contact" id="contact" aria-labelledby="contact-heading" data-reveal="right">
            <p class="contact-key">DIRECT LINE</p>
            <h3 id="contact-heading" class="contact-title">Talk to the people building it.</h3>
            <p class="contact-copy">
                Plans and pricing are still being finalised. Describe your community - how many
                players, how heavily you mod, how you like to play - and we will match it to the
                right world.
            </p>
            <a class="contact-link" href="mailto:@Site.Value.ContactEmail">
                <span>@Site.Value.ContactEmail</span>
                <span class="contact-arrow" aria-hidden="true"></span>
            </a>
        </aside>
    </div>
</section>
